{
  "name": "monorepo-blog",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "start": "pnpm -r start"
  },
  "devDependencies": {
    "typescript": "^4.5.0"
  }
}